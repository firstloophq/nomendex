# 5 - Numbered Lists

Prompt:
- Click Clear all and refresh both tabs so we start fresh.
- Open two browser tabs on the same `/collab-test` doc with distinct users/client IDs.
- In the active page enter a numbered list with one key per stroke:
  - `1. First item`
  - Enter
  - `Second item`
  - Enter
- Wait for sync.
- Take screenshots of both pages.
- Compare output.
- Are both editors displaying the same numbered list content?
