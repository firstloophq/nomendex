# 6 - Bold Toggle (Cmd+B)

Prompt:
- Click Clear all and refresh both tabs so we start fresh.
- Open two browser tabs on the same `/collab-test` doc with distinct users/client IDs.
- In the active page type `bold me` with one key per stroke.
- Select `bold me`.
- Press `Cmd+B` to apply bold.
- Wait for sync.
- Take screenshots of both pages.
- Compare output.
- Are both editors displaying the same bold formatting?
